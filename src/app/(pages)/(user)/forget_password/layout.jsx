import "@app/globals.css";

export const metadata = {
  title: "Forget Password",
  description: "Reset your password for the application",
};

export default function RootLayout({ children }) {
  return <div>{children}</div>;
}
